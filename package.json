{
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "dependencies": {
    "ts-graphviz": "^3.0.7"
  },
  "devDependencies": {
    "fast-check": "^3.22.0",
    "typescript": "^5.9.0",
    "vitest": "^3.1.0"
  },
  "scripts": {
    "build": "tsc --project tsconfig.lib.json && tsc --project tsconfig.json && node build.mjs",
    "build:lib": "tsc --project tsconfig.lib.json",
    "test": "npm run build && vitest run"
  }
}
